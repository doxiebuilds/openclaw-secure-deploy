<!--
Per-project reflection log. Optional per-project template. Typical path:
  /home/node/.openclaw/workspace/memory/projects/memory/MEMORY-<slug>.md

Append only — this is a record of what happened and why, read at the
start of every cycle so mistakes don't repeat. Plain post-mortems, not
narrative. Newest entry at the bottom.

Format per entry:
  ## <date> — cycle <n>
  Tried: <what you attempted>
  Result: <what actually happened — pass/fail/score>
  Why: <the actual cause, not a guess dressed up as certainty>
  Next: <what this changes about the next cycle, if anything>

No example entry is included on purpose: an example post-mortem is
indistinguishable from a real one once this file is instantiated, and
anything reading this file downstream would treat it as history that
happened. Start empty; the first entry should be a real cycle.
-->

